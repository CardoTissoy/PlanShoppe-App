@page "/productsoverview"
@inherits ProductsOverviewClass


<h2 class="page-title"> List of Products</h2><br />
<br />
<button class="btn btn-success" @onclick="(() =>  AddConfirm())"><i class="oi oi-plus"></i> </button>
<br />

@if (_productRepository.ProductList == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class='table'>
        <thead>
            <tr>
                <th>Product Image</th>
                <th>Product Name</th>
                <th>Product Type</th>
                <th>Product Description</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var products in _productRepository.ProductList)
            {
               
                <tr>
                    <td><img src=@products.ProductPhoto width="100" height="100"></td>
                    <td>@products.ProductName</td>
                    <td>@products.ProductType</td>
                    <td>@products.ProductDescription</td>
                    <td>
                        <a class="btn btn-info table-btn" @onclick="(() => ProductDetails(products.ProductId))"><i class="oi oi-info"></i> </a>
                        <a class="btn btn-warning table-btn" @*@onclick="(() => EditEmployee(empproductsloyee.EmployeeId))"*@><i class="oi oi-pencil"></i></a>
                        <a class="btn btn-danger table-btn" @onclick="( () =>  DeleteConfirm(products.ProductId))"> <i class="oi oi-delete"></i></a>
                    </td>
                   
                </tr>
            }
            
        </tbody>
    </table>

    if (isDelete)
    {
        <div class="modal" tabindex="-1" style="display:block" role="dialog">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <h3 class="modal-title">Delete Product</h3>
                    </div>
                    <div class="modal-body">
                        <h4>Do you want to delete this product ??</h4>
                        <table class="table">
                            <tr>
                                <td>Name</td>
                                <td>@Product.ProductName</td>
                            </tr>
  
                        </table>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-danger" onclick="@(async () => await DeleteProductConfirm(Product.ProductId))" data-dismiss="modal">YES</button>
                        <button class="btn btn-warning" @onclick="closeModal">NO</button>
                    </div>
                </div>
            </div>
        </div>
    }
}

@*@if (_productRepository.BlobList  == null )
{
}
else
{
    <table class='table'>
        <thead>
            <tr>
                <th>Product Name</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var blob in _productRepository.BlobList )
            {

                <tr>
                    <td>
                        <img src=@blob.Uri width="100">
                    </td>

                </tr>
            }

        </tbody>
    </table>
}*@